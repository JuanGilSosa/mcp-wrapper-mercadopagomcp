## Review

- Correct: PR3 stays within the approved slice. The implemented PR3 files add a curated seed index, deterministic ranking/search, MCP tool handlers/registration, README docs, and apply progress; I found no MCP resources, arbitrary crawler, live search API, or Q&A synthesis implementation in `src/mercadopago-docs` (`mcp.ts:94-113`, `seed-index.ts:15-50`, `search.ts:51-66`).
- Correct: Search behavior matches the core requirements by inspection: inputs are schema-validated and limit-bounded (`schemas.ts:8-13`), returned indexed URLs are revalidated and de-duplicated before ranking (`search.ts:27-40`), locale/country filters are applied (`search.ts:43-48`), ranking is deterministic with explicit weights and tie sorting (`rank.ts:26-39`), and output contains source-result metadata only (`search.ts:55-65`). Tests cover these paths in `tests/mercadopago-docs/search.test.ts:41-99`.
- Correct: MCP read flow validates before fetch. `read()` calls `parseSafeReadDocInput()` before resolving/fetching (`mcp.ts:60-65`), and URL-shaped inputs pass through allowlist validation in `parseSafeReadDocInput()` (`schemas.ts:36-43`). Tests assert unsafe URL inputs do not call fetch (`tests/mercadopago-docs/mcp.test.ts:90-105`).
- Correct: README documents official-docs-only boundaries, no Q&A synthesis, mocked tests, and safe pnpm commands with `--ignore-scripts` for install/add commands (`README.md:23-43`). The README test enforces that install/add commands include `--ignore-scripts` (`tests/mercadopago-docs/readme.test.ts:7-23`).
- Correct: Verification passed locally: `pnpm test` => 12 files / 81 tests passed; `pnpm exec tsc --noEmit` passed with no output.
- Correct: Strict TDD evidence is plausible and recorded in `openspec/changes/add-mercadopago-docs-mcp/apply-progress.md:135-159`, including RED failures before PR3 modules/README existed, safe install command for `@modelcontextprotocol/sdk`, and final test/tsc verification.

- Blocker: none found.
- High: none found.

- Medium: MCP-facing validation errors for malformed tool inputs are misclassified as `FetchFailed`. `searchDocs()` parses with Zod (`search.ts:51-52`) and `parseSafeReadDocInput()` parses with Zod (`schemas.ts:36-37`); those `ZodError`s are caught by the MCP handler (`mcp.ts:81-89`) and passed to `toMcpError()`, whose non-domain fallback always returns `{ code: 'FetchFailed', message: 'Unexpected Mercado Pago docs failure' }` (`errors.ts:41-50`). This violates the spec/tasks expectation that invalid search/read requests reject with a stable validation error, and it makes client input errors look like network/fetch failures. Requested fix: map Zod validation failures at the MCP boundary to an explicit stable validation-style domain error/code allowed by the project contract, or wrap schema parse failures in `MercadoPagoDocsError` before `toMcpError()`.

- Low: The PR3 review workload is acceptable but close if generated/ongoing artifacts are included. The PR3-relevant source/tests/README/apply-progress files total 858 physical lines by `wc -l`; apply progress alone is 188 lines, and the apply note reports hand-authored PR3 changes at about 573 lines. This is within the approved 800-line hand-authored slice, but reviewers should keep generated lockfile and progress noise out of the effective review budget where possible.

Approval: requested fixes before full approval due to the medium MCP validation-error mapping issue. No blockers or high-severity issues were found.
